# Required by a release build so canonical links and the sitemap use the deployed origin.
SITE_URL=https://docs.example.com
# Use / for an origin deployment or a root-relative mount such as /project/.
BASE_PATH=/
